<%- if remotipart_submitted? %>
  var html = "<%= escape_javascript(render(partial: 'attachments/cover', locals: {cover: @user.cover})) %>";
  $('.cover-image-container').remove()
  $('section:first').prepend(html);
<% end %>

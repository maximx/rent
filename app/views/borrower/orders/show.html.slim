= render_borrower_tablist

.tab-content
  h1 = "##{@order.id} #{render_datetime_period(@order, :date_only)}"
  = render partial: 'lender/records/index', locals: {lender_records: @lender_records, record_state_log: @record_state_log}

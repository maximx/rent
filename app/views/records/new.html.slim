- breadcrumb :new_record, @item

= render partial: 'items/picture_list', locals: {item: @item}

h3.col-md-offset-3 = t('controller.records.action.new')
= render partial: 'records/form', locals: { item: @item, event_sources_path: @event_sources_path,
                                            record: @record, disabled_dates: @disabled_dates }

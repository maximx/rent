= render layout: 'layouts/head' do
  #site-content role="main"
    = yield :full_top
    .container
      = render_notice(notice)
      = render_alert(alert)
      = raw breadcrumbs style: :bootstrap, autoroot: false
      = yield
    = yield :full_bottom

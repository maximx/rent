<%= render_link_li class: "nav nav-sidebar" do |li|
      li << ["基本資料", user_path]
      li << ["出租物", items_user_path]
      li << ["出租評價", lender_reviews_user_path]
      li << ["承租評價", borrower_reviews_user_path]
      li << ["追蹤", following_user_path]
      li << ["收藏", items_collect_user_path]
    end %>

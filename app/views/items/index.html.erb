<div class="clearfix">
  <%= link_to("新增出租物", new_item_path, class: "pull-right btn btn-danger") %>
</div>

<table>
  <thead>
    <td>名稱</td>
  </thead>
  <tbody>
  <% @items.each do |item| %>
    <tr>
      <td><%= link_to(item.name, item_path(item)) %></td>
    </tr>
  <% end %>
  </tbody>
</table>

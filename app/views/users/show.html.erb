<div>
  <%= render partial: "profiles/profile", locals: { user: @user, profile: @profile } %>

  <!-- Nav tabs -->
  <ul class="nav nav-tabs" role="tablist">
    <li role="presentation" class="active"><a href="#items" aria-controls="items" role="tab" data-toggle="tab">出租物</a></li>
    <li role="presentation"><a href="#requirements" aria-controls="requirements" role="tab" data-toggle="tab">求租物</a></li>
    <li role="presentation" class="dropdown">
      <%= link_to("#", class: "dropdown-toggle",
                       id: "reviews-dropdown",
                       data: { toggle: :dropdown },
                       aria: { controls: "reviews-tabs" }) do %>
        評價
        <span class="caret"></span>
      <% end %>
      <ul class="dropdown-menu" role="menu" aria-labelledby="reviews-dropdown" id="reviews-content">
        <li>
          <%= link_to("承租評價", "#borrower-reviews", id: "borrower-dropdown", tabindex: "-1", role: "tab", data: { toggle: "tab" }, aria: { controls: "borrower-reviews"}) %>
        </li>
        <li>
          <%= link_to("出租評價", "#lender-reviews", id: "lender-dropdown", tabindex: "-1", role: "tab", data: { toggle: "tab" }, aria: { controls: "lender-reviews"}) %>
        </li>
      </ul>
    </li>

    <li role="presentation" class="dropdown">
      <%= link_to("#", class: "dropdown-toggle",
                       id: "like-dropdown",
                       data: { toggle: :dropdown },
                       aria: { controls: "like-tabs" }) do %>
        喜愛
        <span class="caret"></span>
      <% end %>
      <ul class="dropdown-menu" role="menu" aria-labelledby="like-dropdown" id="like-content">
        <li>
          <%= link_to("追蹤", "#user-follow", id: "following-dropdown", tabindex: "-1", role: "tab", data: { toggle: "tab" }, aria: { controls: "user-follow"}) %>
        </li>
        <li>
          <%= link_to("收藏", "#item-collect", id: "collecting-dropdown", tabindex: "-1", role: "tab", data: { toggle: "tab" }, aria: { controls: "item-collect"}) %>
        </li>
      </ul>
    </li>
  </ul>

  <div class="tab-content">
    <div role="tabpanel" class="tab-pane active" id="items">
      <%= render partial: "items/item", collection: @user.items, as: :item %>
    </div>

    <div role="tabpanel" class="tab-pane" id="requirements">
      <% @user.requirements.each do |requirement| %>
        <%= link_to(requirement.name, requirement_path(requirement)) %>
        <%= requirement.price %>
      <% end %>
    </div>

    <div role="tabpanel" class="tab-pane" id="borrower-reviews">
      <%= render partial: "reviews/review", collection: @user.borrower_reviews, as: :review %>
    </div>

    <div role="tabpanel" class="tab-pane" id="lender-reviews">
      <%= render partial: "reviews/review", collection: @user.lender_reviews, as: :review %>
    </div>

    <div role="tabpanel" class="tab-pane" id="user-follow">
    <% @user.following.each do |follow| %>
      <div><%= render_cl_avatar_small(follow) %></div>
      <div><%= link_to(follow.email, user_path(follow)) %></div>
      <div><%= render_follow_link(follow) if current_user != follow %></div>
    <% end %>
    </div>

    <div role="tabpanel" class="tab-pane" id="item-collect">
      <%= render partial: "items/item", collection: @user.collections, as: :item %>
    </div>
  </div>

</div>

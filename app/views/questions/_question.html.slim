article.question
  .row
    .ask.clearfix
        .col-lg-2.col-lg-offset-1.text-center
          = link_to user_url(question.asker) do
            = render_avatar_thumb(question.asker)
            div = question.asker.account
        .col-lg-9 問題∶ #{ question.content }

    - if question.reply?
      .reply.clearfix
          .col-lg-1.col-lg-offset-2.text-center
            = link_to user_url(question.item.lender) do
              /TODO: lender.avatar
              = question.item.lender.account
              div = question.item.lender.account
          .col-lg-9 回覆∶#{ question.reply }

    - elsif question.replied_by?(current_user)
      .reply.row
        = simple_form_for [question.item, question] do |f|
          = f.input :reply

          .form-group
            .col-xs-offset-4.col-xs-8.col-sm-offset-3.col-sm-9
              = f.button :submit, "回覆"
